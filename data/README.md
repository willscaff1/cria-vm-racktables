# Pasta data

Esta pasta guarda estado local de runtime.

Arquivos reais como `racktables.json`, `vcenters.json`, `jobs.json`, logs e payloads de teste nao devem ser commitados, porque podem conter credenciais, IPs internos e historico operacional.

Use os arquivos `*.example.json` como referencia de formato.

